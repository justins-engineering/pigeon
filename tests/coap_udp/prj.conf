CONFIG_ZTEST=y

# zephyr/net/coap.h (COAP_TYPE_*/COAP_CODE_EMPTY, all this unit consumes) is
# header-only at these usage sites, but its inclusion chain still wants the
# net_ip type definitions that CONFIG_NETWORKING gates the build of.
CONFIG_NETWORKING=y
CONFIG_TEST_RANDOM_GENERATOR=y

CONFIG_ZTEST=y

# Real settings-on-NVS persistence (native_sim's own storage_partition), so
# the record save/load/clear round-trip is exercised against the same
# subsystem the library uses on hardware -- not a mock.
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y
CONFIG_NVS=y
CONFIG_SETTINGS=y
CONFIG_SETTINGS_NVS=y
